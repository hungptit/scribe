#pragma once

#include <cstring>

namespace scribe {
    struct CSVPolicy {
        void operator()(const char *begin, const size_t len) {
            fmt::print("TODO: Implement this method!");
        }
    };
} // namespace scribe
